# Default rotation: portrait (no rotation).
# First boot runs the orientation selection dialog in portrait so touch
# always works. set-display-rotation.sh overwrites this on every boot.
QT_QPA_EGLFS_ROTATION=0
