# Yocto build directory: everything here is regenerated by bitbake, except
# build/conf/ (checked in and pre-configured, see README.md) and
# build/scripts/ (host-workaround wrappers referenced from local.conf)
build/*
!build/conf/
!build/scripts/

# Python bytecode
__pycache__/
*.pyc
